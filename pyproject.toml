[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "CORE_llm"
version = "0.1"

[tool.setuptools]
dependencies = {file = ["requirements.txt"]}
py-modules = []
